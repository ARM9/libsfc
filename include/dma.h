#ifndef _LIBSFC_DMA#define _LIBSFC_DMA#include "sfc_regs.h"//DMA/***args:source pointerdestination index into CGRAM 0-255size in bytes, 2 bytes per color*/extern void loadPalette(unsigned int dest, void *source, size_t size);void dmaToCGRAM(unsigned int dest, void *source, size_t size);//HDMA/*channel: 1-7mode_dest:	low byte: hdma mode			high byte: destination register 0x21xxsource_address: 16-bit address of source tablesource_bank: bank of source table*/extern void setupHDMA(unsigned char channel, unsigned int mode_dest, unsigned int source);// uint16_t source_address, uint8_t source_bank);#endif